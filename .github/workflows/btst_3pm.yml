name: NIFTY BTST 3PM Signal

on:
  workflow_dispatch:

  schedule:
    # 15:00 IST = 09:30 UTC
    - cron: "30 9 * * 1-5"

permissions:
  contents: write

jobs:
  btst-signal:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run test suite
        run: python -m pytest -q

      - name: Generate 3 PM BTST signal
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python main.py --mode 3pm

      - name: Persist BTST position state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/live_btst_signal.json

          if git diff --cached --quiet; then
            echo "No BTST position state to commit."
          else
            git commit -m "Persist BTST position state"
            git push
          fi